name: Frames v108 r50 USB Device Endpoint Status Proof Certification

on:
  workflow_dispatch:
  push:
    branches: [ v108-usb-hub-topology-r1 ]
    paths:
      - '.github/workflows/frames-v108-r50-usb-device-endpoint-status-cert.yml'
      - 'tools/ci/patch_v108_r50_device_endpoint_status_proof.py'
      - 'tools/ci/r50_cert_driver.py'
      - 'tools/ci/patch_v108_r49_xhci_root_port_slot_proof.py'
      - 'tools/ci/r49_cert_driver.py'
      - 'tools/ci/patch_v108_r48_xhci_scheduler_wake.py'
      - 'tools/ci/r48_cert_driver.py'
      - 'tools/ci/patch_v108_r47_xhci_doorbell_flush_probe.py'
      - 'tools/ci/patch_v108_r46_xhci_periodic_context_proof.py'
      - 'tools/ci/patch_v108_r45_touchpad_button_isolation_xhci_dcs.py'
      - 'tools/ci/r45_cert_driver.py'

permissions:
  contents: read
  actions: read
  statuses: write

jobs:
  certify:
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v108-usb-hub-topology-r1
      - name: Publish r50 pending status
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TARGET="https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          python3 - "$TARGET" <<'PY'
          import json,sys
          open('/tmp/status-pending.json','w').write(json.dumps({'state':'pending','context':'frames/r50-certification','description':'r50 certification running','target_url':sys.argv[1]}))
          PY
          curl -fsSL -X POST \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/statuses/${GITHUB_SHA}" \
            --data-binary @/tmp/status-pending.json
      - name: Install build, image and runtime tools
        run: |
          sudo apt-get update
          sudo apt-get install -y clang lld python3 python3-pil unzip mtools dosfstools gdisk xorriso qemu-system-x86 ovmf
      - name: Fetch exact r21 protected source
        uses: actions/download-artifact@v4
        with:
          name: Frames-v108-r21-Candidate
          path: r21-candidate
          github-token: ${{ secrets.GITHUB_TOKEN }}
          repository: afterburn25/frames
          run-id: '31979817129'
      - name: Harden TCG text readiness harness
        run: python3 tools/ci/harden_qemu_text_edit_gate_r28.py
      - name: Build and certify r50
        run: python3 tools/ci/r50_cert_driver.py
      - name: Validate sealed Rufus physical handoff
        run: python3 tools/ci/validate_physical_handoff.py --final final
      - name: Upload r50 evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Frames-v108-r50-Evidence
          path: |
            evidence/**
            out/*.iso
          if-no-files-found: warn
          retention-days: 30
      - name: Upload sealed r50 Rufus ISO
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: Frames-v108-r50-Rufus-Final
          path: final/**
          if-no-files-found: error
          retention-days: 30
      - name: Publish r50 certification status
        if: always()
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          JOB_STATUS: ${{ job.status }}
        run: |
          STATE=success
          DESC='r50 certification PASS'
          if [ "$JOB_STATUS" != "success" ]; then STATE=failure; DESC='r50 certification FAIL'; fi
          TARGET="https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          python3 - "$STATE" "$DESC" "$TARGET" <<'PY'
          import json,sys
          open('/tmp/status.json','w').write(json.dumps({'state':sys.argv[1],'context':'frames/r50-certification','description':sys.argv[2],'target_url':sys.argv[3]}))
          PY
          curl -fsSL -X POST \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/statuses/${GITHUB_SHA}" \
            --data-binary @/tmp/status.json
