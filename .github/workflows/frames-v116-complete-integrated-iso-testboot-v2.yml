name: Frames v116 Complete Integrated ISO Test Boot v2 (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Superseded workflow notice
        run: |
          set -euo pipefail
          echo 'This v2 workflow is retired.'
          echo 'Use frames-v116-complete-integrated-iso-testboot-v3.yml for the fail-closed UEFI El Torito ISO boot proof.'
