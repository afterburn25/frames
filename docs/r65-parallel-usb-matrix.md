Frames v108 r65 parallel USB repair matrix

Four fail-closed EHCI variants (A-D) are built in parallel by GitHub Actions. The matrix must fail rather than silently patch an uncertain source when expected EHCI anchors are absent.
